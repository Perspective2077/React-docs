For admin ofc. 
Add Re-rendering of componentn , when change state will the running js code run after the re-render or before the re-render
need to add functional updater and whats the diff between normal updating and function updating in usestate
